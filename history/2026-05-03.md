# 2026-05-03 项目更改记录（北京时间）

## 00:53

### 修改内容描述

- 新增 `examples/lark-cli-rag-samples.jsonl`：
  - 专门用于演示结构化规则未命中后进入本地轻量 RAG / retriever 流程。
  - 样本覆盖 IM、Base、Drive 导入、Docs/Sheets 下钻、Calendar 会议室流程、user/bot 身份选择等典型 Skill 路由场景。
  - 样本文本刻意避开现有规则强信号，便于验证 `retriever` 分支。
- 更新 `docs/next_plan.md`：
  - 记录 RAG 演示样本文件、覆盖范围和验证命令。

### 修改原因

- 当前已有规则命中样本，但缺少专门证明“规则未命中 -> 本地知识召回 -> 召回证据卡片”的样例。
- 比赛强调 Agent 能主动寻找内部知识，而不只是固定规则匹配；需要用独立样例展示 retriever 的价值。

### 影响范围

- 不影响现有运行入口和业务逻辑。
- 新增样例可通过 `npm run demo -- --source examples/lark-cli-rag-samples.jsonl --no-quality-report` 验证。
- 运行验证会更新 `tmp/` 下的临时产物。

## 01:20

### 修改内容描述

- 放宽事件监听与轮询触发条件：
  - `src/chatEventMain.js`
  - `src/chatPollMain.js`
  - 将原本偏“报错型”的 `looksLikeCliError()` 调整为同时识别“报错 + 问题式提问 + 规则咨询”。
  - 新增对 `应该`、`怎么选`、`先看`、`参考`、`哪类`、`哪个`、`还是`、`是否`、`建议`、`规则`、`说明`、`流程`、`选哪` 等询问语气的支持。
  - 保留 `lark-cli` 关键词约束，避免泛化到非 CLI 话题。
- 目的：让飞书群里类似“我应该看哪类认证规则”的提问也能进入知识卡流程，避免只认报错文本导致事件收到了但知识卡没推送。

### 修改原因

- 当前测试群里的消息是“询问型”而不是“报错型”，原过滤条件过窄，导致事件收到了但在进入 RAG/卡片前被跳过。
- 方向 C 要求的是“沉浸式碎片知识推送助手”，应覆盖开发者在群里直接提问“该看哪个 Skill / 该选哪个身份 / 该先查什么”的场景。

### 影响范围

- 影响事件监听与轮询两条主动触发链路的进入条件。
- 不影响知识匹配、RAG 检索、卡片渲染、推送接口和运行入口。
- 已通过 `node --check` 验证，并用本地 RAG 样例验证知识卡仍能正常生成。

## 01:06

### 修改内容描述

- 重构 `src/adapters/output/terminalCard.js` 的终端知识卡片展示格式：
  - 标题改为 `CLI Knowledge Card · Debug View`。
  - 增加流水线摘要，展示 `规则命中/RAG召回 -> 分类 -> 工具计划 -> 执行模式 -> 结果状态`。
  - 将输出拆为 7 个阶段：输入上下文、知识获取与匹配、诊断与建议、Agent 动作选择、工具计划、执行结果、分发与来源。
  - RAG 路径下展示召回策略、召回原因、召回数量和 Top3 召回证据。
  - 工具执行详情仅在存在预检、schema、help 或分析结果时展示，避免空段落干扰调试。
- 修复 `src/core/knowledge/retriever.js` 的命令抽取细节：
  - 避免从被截断的 overview 文本中抽出 `lark-cli sche` 这类半截命令。
  - 对抽出的 `lark-cli` 命令做基础合理性校验。

### 修改原因

- 原终端知识卡片将触发、匹配、RAG、决策、工具计划、执行结果和来源混在一起，不利于观察每个流程是否按预期执行。
- RAG 样例调试时需要清楚区分“规则直接命中”和“规则未命中后本地知识召回”。

### 影响范围

- 影响终端本地 Demo 的可读性输出。
- 不影响飞书卡片 payload、运行入口和核心决策逻辑。
- 可通过 `npm run demo -- --source examples/lark-cli-rag-samples.jsonl --no-quality-report` 和 `npm run demo -- --source examples/lark-cli-error-samples-small.jsonl --no-quality-report` 验证。

## 01:46

### 修改内容描述

- 使用 `lark-cli docs +fetch` / `docs +update` 更新飞书周报文档 `XuPydaV9ko8vEGxwY3oc8HFvnLb` 的“第三周期（4.29-5.1）”内容。
- 按既有结构补写了四个小节：
  - 一、核心产出
  - 二、量化指标
  - 三、过程复盘与沉淀
  - 四、本周随手记
- 内容围绕本周期真实进展展开，重点覆盖事件监听修复、本地知识检索接入、RAG 样例补充、终端知识卡片可观察性增强等工作。

### 修改原因

- 当前比赛周报文档中第三周期仍是模板占位，缺少真实研发进展记录。
- 需要基于已完成的项目状态，补齐阶段性成果、量化指标和过程复盘，方便后续继续完善第四、第五周期以及最终效果验证报告。

### 影响范围

- 影响飞书文档中的项目周报内容，不涉及仓库内业务代码逻辑修改。
- 本次写入通过 `lark-cli` 官方接口完成，并已用 `docs +fetch` 回读确认内容生效。

## 21:43

### 修改内容描述

- 新增 `src/cliWatchMain.js`，补齐方向 C 要求的 CLI 终端监听入口：
  - 支持驻留式 Agent Shell，用户在该会话内执行命令。
  - 支持 `--command` 单命令模式，方便自动化测试和 Demo 录屏。
  - 收集命令文本、退出码、stdout/stderr 摘要、当前工作目录和耗时。
  - 对失败的 `lark-cli` 命令自动触发知识卡；对 `auth`、`schema`、`--help` 等特定只读命令支持成功态知识提示。
- 新增 `src/app/processKnowledgeEvent.js`，沉淀可复用的知识事件处理流程：
  - 结构化规则匹配。
  - 规则未命中时本地轻量 RAG 召回。
  - 决策、工具计划、终端卡片、飞书卡片推送和本地 artifact 输出。
- 更新 `src/core/agent/decisionEngine.js`，让知识卡触发模式读取事件自身的 `trigger_mode`，终端监听会显示为 `cli_watch`。
- 更新 `package.json`，新增 `npm run demo:cli-watch`。
- 更新 `README.md` 与 `docs/product-definition.md`，补充终端监听运行方式、能力边界和比赛方向 C 对齐说明。

### 修改原因

- 参赛方向 C 明确要求“驻留在 CLI 终端”的实时知识点推送助手；此前项目已有飞书群消息监听与推送链路，但缺少真正的终端侧主动触发入口。
- 需要把当前系统从“群消息触发知识卡”补齐为“开发者在 CLI 中执行命令 -> 命令失败或命中特定命令 -> 主动推送知识卡”的完整方向 C 链路。

### 影响范围

- 新增一个运行入口，不改变现有 `demo`、`demo:chat-poll`、`demo:chat-event` 的行为。
- 终端监听默认只分析带 `lark-cli` 上下文的命令，避免干扰普通 shell 操作。
- 已在容器中通过 `node --check` 验证，并完成两条端到端测试：
  - `lark-cli wiki --unknown-flag`：规则命中，生成 `cli_watch` 知识卡和 `lark-cli wiki --help` 工具计划。
  - `lark-cli im --help`：成功态特定命令触发，本地 RAG 召回 `lark-im` Skill 证据。

## 23:25

### 修改内容描述

- 将 `demo:cli-watch` 进一步收敛为更像真实终端的 `demo:cli-shell` 入口：
  - `package.json` 新增 `demo:cli-shell` 别名。
  - `README.md` 将该能力统一描述为 CLI Shell，而不是单纯测试命令。
- 增强 `src/cliWatchMain.js` 的交互体验：
  - 新增基础会话态支持：`cd`、`pwd`、`clear`、`exit`。
  - Prompt 显示当前工作目录，Shell 更接近真实终端使用方式。
  - 非 TTY 场景下支持从 stdin 顺序读取命令，便于自动化测试和录屏脚本。
  - 修复 stdin 关闭时的 `readline was closed` 报错。
  - 根据平台自动选择默认 shell：Windows 使用 `cmd.exe` / `ComSpec`，Linux 使用 `$SHELL` 或 `/bin/bash`。
- 新增启动脚本：
  - `scripts/agent-shell.ps1`
  - `scripts/agent-shell.sh`

### 修改原因

- 用户需要的不是单次 `--command` 回放，而是一个可以接收本地命令输入、具备基本目录切换能力、真实承接 CLI 使用场景的终端会话。
- 需要让项目在比赛演示时更像“驻留在终端里的知识助手”，而不是“跑一个测试命令生成卡片”。

### 影响范围

- 交互模式更接近真实终端，仍保留 `--command` 作为自动化测试入口。
- 已验证 Linux 容器中的交互回归：
  - `pwd`
  - `cd /workspace`
  - `lark-cli wiki --unknown-flag`
- 已验证 Windows 启动脚本 `scripts/agent-shell.ps1 --command "lark-cli im --help"` 不再错误引用 `/bin/bash`。

## 23:43

### 修改内容描述

- 新增“更像默认 `lark-cli`”的代理入口：
  - `src/larkCliProxyMain.js`
  - `package.json` 新增 `demo:lark-cli-proxy`
  - `scripts/lark-cli-agent.ps1`
  - `scripts/lark-cli-agent.sh`
- 代理模式支持：
  - `--agent` 之前的参数作为真实 `lark-cli` 参数执行
  - `--agent` 之后的参数传给知识监听层，例如推送、去重、调试等设置
  - 保留原始命令退出码，便于脚本、CI 或自动化链路继续判断命令成功与否
- 更新 `README.md` 与 `docs/product-definition.md`，补充代理模式的用法和定位。

### 修改原因

- 虽然 CLI Shell 已经能接收本地命令，但用户仍需要先进入 Shell 会话；为了更贴近“默认 `lark-cli` 多一层知识感知”的产品体验，需要提供代理式命令执行入口。
- 方向 C 的理想演示方式，不只是“进入一个 Agent Shell”，也应该支持“像原来一样敲 `lark-cli`，但失败时自动推知识卡”。

### 影响范围

- 新增一条更贴近日常使用的运行入口，不影响现有 `demo:cli-shell`、`demo:cli-watch`、`demo:chat-event` 和 `demo:chat-poll`。
- 已验证容器中的代理链路：
  - `npm run demo:lark-cli-proxy -- wiki --unknown-flag --agent --push-level all --push-bypass-dedupe`
  - `npm run demo:lark-cli-proxy -- im --help --agent --push-lark-card --push-chat-id "oc_d32c2e3e2eb66b2efca3ef677620233e" --push-as bot --push-level all --push-bypass-dedupe`
- 已验证 Windows 本地脚本：
  - `scripts/lark-cli-agent.ps1 im --help --agent --push-level all`
