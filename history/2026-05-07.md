## 10:10 调整 Markdown 分块策略，按结构切块并增加重叠上下文

- 修改内容：
  - 调整 `src/core/knowledge/retriever.js` 的 `splitMarkdownSections()`，为 Markdown section 记录：
    - `heading`
    - `headingPath`
    - `level`
  - 调整 `markdownToChunks()`：
    - chunk 标题优先使用完整 `headingPath`
    - metadata 中补充 `sectionPath`、`headingLevel`
    - skill 文档长 section 切分时启用 `overlapParagraphs: 1`
  - 调整 `splitLongText()`，支持通过 `overlapParagraphs` 配置段落级 overlap。
  - 调整 `splitDocContent()`，让云文档长块切分时也使用一段 paragraph overlap，和 skill 文档行为保持一致。
- 修改原因：
  - 用户建议 skill 文件更适合按 Markdown 结构切块，并在长块切分时加入块重叠，减少跨段信息断裂。
  - 旧实现虽然已经按标题分 section，但没有保留标题层级，也没有 overlap，长段被硬切后上下文衔接较弱。
- 影响范围：
  - 影响本地 skill 和云文档 chunk 的构建方式。
  - 不改变主检索接口，只增强 chunk 的结构信息和上下文连续性。
- 验证：
  - 已通过 `node --check src/core/knowledge/retriever.js`
  - 已做最小采样检查，chunk metadata 已包含 `sectionPath` / `headingLevel`

## 10:35 将 CLI Shell 知识分析改为后台队列模式

- 修改内容：
  - 重写 `src/cliWatchMain.js`，新增 `analysisMode` 运行模式：
    - `background`
    - `blocking`
  - 交互式 `cli-shell` / `cli-watch` 默认启用 `background`，命令执行完成后立即归还 shell，再由后台串行队列异步完成知识分析、LLM 整理和飞书卡片推送。
  - 新增后台任务队列与退出等待逻辑：
    - `createAnalysisState()`
    - `enqueueAnalysisJob()`
    - `runAnalysisJob()`
    - `waitForPendingAnalyses()`
  - `--command` 单命令模式结束前会等待后台任务完成，避免进程过早退出导致看不到知识卡。
  - 在 `README.md` 中补充 `--analysis-mode <background|blocking>` 参数说明。
- 修改原因：
  - 用户反馈 `cli-shell` 在召回和 LLM 整理期间会明显卡顿，影响开发人员连续输入命令的效率。
  - 后台队列更符合真实开发场景：命令前台执行，知识卡异步补充。
- 影响范围：
  - 影响 `demo:cli-watch`、`demo:cli-shell`、`demo:competition:cli` 的交互体验。
  - 不改变 matcher / retriever / composer / push 主逻辑，只改入口调度方式。
- 验证：
  - 已通过 `node --check src/cliWatchMain.js`
## 10:55 撤回 CLI 启动暖机改造，恢复先初始化再进入 shell

- 修改内容：
  - 将 `src/cliWatchMain.js` 恢复为“先加载知识库与检索器，再进入交互 shell”的启动顺序。
  - 保留上一轮已经验证通过的后台分析队列能力，不撤回 `analysisMode=background|blocking`。
  - 清理 `cliWatchMain` 中的乱码摘要文本，统一使用正常中文日志。
- 修改原因：
  - 用户确认当前“未进入输入界面”的等待主要来自 embedding 暖机，而不是启动逻辑缺陷。
  - 因此不继续保留“先起 shell 后暖机”的改造，避免入口行为在比赛演示阶段变得更复杂。
- 影响范围：
  - 恢复 `demo:cli-shell` / `demo:cli-watch` 的原始启动体验：等待初始化完成后再显示 prompt。
  - 后台分析队列仍然生效，命令执行后的知识分析依旧不会阻塞 shell。
- 验证：
  - 已通过 `node --check src/cliWatchMain.js`

## 11:20 收敛终端输出，release 模式仅显示核心结论

- 修改内容：
  - 重写 `src/adapters/output/terminalCard.js`，将终端输出分为两种视图：
    - `release`：仅输出核心结论、建议命令、关联 skill、首条证据和状态摘要
    - `debug`：保留完整的分步知识卡视图
  - 调整 `src/app/processKnowledgeEvent.js` 与 `src/app/runDemo.js`，将 `options.cardView` 传入终端渲染层。
  - 清理 `terminalCard.js` 中原有乱码字符串，统一使用正常中文。
- 修改原因：
  - 用户反馈开发场景下终端打印过多细节，会影响连续开发与阅读效率。
  - 终端更适合“结论优先”，详细证据与检索过程只在 debug 排障时显示。
- 影响范围：
  - 影响本地终端卡片展示，不影响飞书卡片内容。
  - `cardView=release` 现在真正变成精简视图；`cardView=debug` 才会打印长卡。
- 验证：
  - 已通过 `node --check src/adapters/output/terminalCard.js`
  - 已通过 `node --check src/app/processKnowledgeEvent.js`
  - 已通过 `node --check src/app/runDemo.js`

## 11:35 收敛飞书 release 卡片文案，隐藏内部检索过程

- 修改内容：
  - 调整 `src/adapters/output/larkCardPayload.js` 的 release 视图文案：
    - 移除“信号”字段，避免在正式卡片里暴露 `retriever:...` 内部标识。
    - 将“命中依据”改为“参考资料”，减弱调试色彩。
    - 新增 `sanitizeReleaseDiagnosis()`，在 release 卡片中自动去掉“结构化规则未直接命中 / 已从本地知识库召回 ...”这类内部流程描述。
- 修改原因：
  - 用户反馈当前飞书 release 卡片仍残留较强的调试味，不够像面向开发者的正式结论。
- 影响范围：
  - 只影响飞书卡片 release 视图的展示文案。
  - debug 视图仍保留完整检索与诊断过程。
- 验证：
  - 已通过 `node --check src/adapters/output/larkCardPayload.js`

## 11:50 整理比赛演示脚本，重写 docs/演示文件.md

- 修改内容：
  - 重写 `docs/演示文件.md`，统一为 UTF-8 中文版本。
  - 固定三条主链路命令：
    - CLI 主链路
    - 云知识 RAG 链路
    - 飞书群事件主链路
  - 增加固定问题样例、CLI 终端测试案例、综合评测命令与推荐演示顺序。
- 修改原因：
  - 用户确认项目已接近收尾，希望把演示材料整理为“可直接照着跑、照着讲”的最终版脚本。
- 影响范围：
  - 只影响演示文档，不影响代码逻辑。
- 验证：
  - 已人工检查文件内容与当前命令体系一致。

## 12:40 重写 README，收敛为复赛提交版说明

- 修改内容：
  - 重写 `README.md`，清理历史乱码与过期描述。
  - README 现在按复赛交付视角组织：
    - 项目定位与核心能力
    - 主链路说明
    - 目录结构
    - `.env` 推荐配置
    - 飞书群事件 Demo
    - CLI Shell Demo
    - 云知识 RAG Demo
    - `eval:quality` / `eval:all` 评测证明
    - 常用脚本与参数速查
    - 云知识 manifest 示例
  - 明确推荐比赛演示使用 `hybrid-vector`、`release` 卡片、SDK WebSocket 事件源和 LLM Composer。
- 修改原因：
  - 用户准备复赛作品提交材料，需要 README 更贴近当前项目版本和录屏演示路径。
  - 原 README 混有历史阶段内容和乱码，已经不适合作为复赛项目门面。
- 影响范围：
  - 仅文档更新，不影响代码逻辑。
- 验证：
  - 已人工检查 README 结构与当前 npm scripts、主要参数保持一致。

## 12:20 整理复赛核心代码展示材料

- 修改内容：
  - 在以下核心文件中补充展示型注释，方便截图时直接说明模块职责：
    - `src/app/processKnowledgeEvent.js`
    - `src/chatEventMain.js`
    - `src/cliWatchMain.js`
    - `src/core/knowledge/retriever.js`
    - `src/core/agent/llmComposer.js`
    - `src/adapters/output/larkCardPayload.js`
    - `src/adapters/output/terminalCard.js`
  - 新增 `docs/core-code-showcase.md`，整理复赛材料“核心部分代码展示”的推荐截图位置、展示重点和可直接放入材料的说明文案。
- 修改原因：
  - 用户需要填写复赛作品提交材料中的“核心部分代码展示”，希望结合当前项目挑出最能体现 Agent 工程链路的关键代码。
  - 这部分需要突出主动触发、统一知识处理、混合检索、LLM 证据压缩和飞书/终端卡片分发，而不是简单罗列文件。
- 影响范围：
  - 只新增注释和文档说明，不改变业务逻辑。
- 验证：
  - 已通过 `node --check src/app/processKnowledgeEvent.js`
  - 已通过 `node --check src/cliWatchMain.js`
  - 已通过 `node --check src/chatEventMain.js`
  - 已通过 `node --check src/core/knowledge/retriever.js`
  - 已通过 `node --check src/core/agent/llmComposer.js`
  - 已通过 `node --check src/adapters/output/larkCardPayload.js`
  - 已通过 `node --check src/adapters/output/terminalCard.js`
## 提交边界清理

- 修改内容：
  - 更新 `.gitignore`，忽略本地密钥、运行时缓存、评测输出、日志和 `tmp/` 目录。
  - 使用 `git rm -r --cached tmp` 将已跟踪的运行时产物从 Git 索引移除，但保留本地文件。
  - 将 `.vscode/launch.json` 从 Git 索引移除；该文件是本机调试配置，且包含旧样例路径，不适合作为最终提交内容。
- 修改原因：
  - `tmp/` 中包含飞书文档缓存、向量缓存、评测报告、推送去重状态和日志，属于可重新生成的运行产物，不适合作为最终提交内容。
  - 特别是 `tmp/retriever-vector-store.json` 体积较大，不应进入项目提交。
- 影响范围：
  - 不影响源码、知识库、docs、examples 和 package-lock。
  - 后续运行评测或 demo 会重新生成 `tmp/` 下的文件，但不会再被 Git 跟踪。

## 复现说明补强

- 修改内容：
  - 更新 `README.md`，补充前置条件、`.env.example` 复制方式、推荐复现顺序。
  - 明确飞书群事件监听需要企业自建应用、长连接事件订阅、`im.message.receive_v1` 和机器人入群权限。
  - 将快速开始拆为本地样例、质量评测、真实主动触发三步，方便新接手者逐步验证。
  - 更新 `.env.example`，对齐 README 中的 hybrid-vector、embedding、card view、LLM timeout 等推荐配置。
- 修改原因：
  - 用户希望确认 README 是否足够支撑别人复现项目。
  - 原 README 主体完整，但缺少前置环境和最小验证顺序，`.env.example` 也和当前推荐模型/超时配置不完全一致。
- 影响范围：
  - 仅文档和环境变量模板更新，不影响运行逻辑。
