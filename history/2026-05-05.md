## 10:10

### 修改内容描述

- 为了接入用户上传在飞书云空间文件夹里的 `calendar` skill 文档，扩展了云知识清单能力：
  - `knowledge/lark-cloud-docs.json` 新增 `folders[]`
  - `knowledge/lark-cloud-docs.example.json` 增加文件夹示例
  - 新增 `knowledge/lark-cloud-docs.calendar.json`，预置当前 calendar 文件夹链接
- 更新 `src/adapters/knowledge-source/larkDocsKnowledgeSource.js`
  - 新增 `expandLarkDocsManifest()`
  - 新增 `listLarkDriveFolderDocuments()`
  - 支持通过 `lark-cli drive files list` 把 Drive folder 展开成 `doc/docx/wiki` 文档源
- 更新 `src/core/knowledge/retriever.js`
  - 解析 `knowledge/lark-cloud-docs*.json` 时同时处理 `docs[]` 和 `folders[]`
  - 文件夹中的云文档会自动进入 retriever chunk 构建链路
- 更新 `README.md` 和 `docs/product-definition.md`
  - 补充“云文档文件夹级接入”的使用说明和产品表述

### 测试与验证

- 已验证静态代码：
  - `node --check src/adapters/knowledge-source/larkDocsKnowledgeSource.js`
  - `node --check src/core/knowledge/retriever.js`
- 已尝试验证当前 calendar 文件夹：
  - `lark-cli drive files list --params '{"folder_token":"I7w5fa75ylE0QUdswB9c0oLinnf"}' --as user --format json`
- 当前受阻原因：
  - 缺少 scope：`space:document:retrieve`
  - `docs +search` 额外缺少 scope：`search:docs:read`
- 已生成新的设备流授权链接，待用户完成浏览器授权后，即可继续执行 folder 枚举与实际 RAG 接入验证

## 10:45

### 修改内容描述

- 为了让飞书云文档入口更适合实际使用，给 `src/adapters/knowledge-source/larkDocsKnowledgeSource.js` 增加了本地缓存：
  - 默认缓存文件：`tmp/lark-docs-cache.json`
  - 默认 TTL：1 小时
- 调整 `src/core/knowledge/retriever.js`
  - `cloud_doc` 的召回权重略高于普通本地 skill，便于在“本地同名 skill + 云端同主题文档”同时存在时，更容易看到云端来源
- 新增专门的 calendar 云文档样例：
  - `examples/lark-cloud-calendar-samples.jsonl`
- 更新 `README.md`
  - 补充缓存说明
  - 补充 calendar 云文档演示命令

### 测试与验证

- 已验证：
  - `node --check src/adapters/knowledge-source/larkDocsKnowledgeSource.js`
  - `node --check src/core/knowledge/retriever.js`
  - `npm run demo -- --source examples/lark-cli-rag-samples.jsonl --no-quality-report`
- 说明：
  - 当前环境仍因缺少 `space:document:retrieve` 无法直接完成真实 folder 枚举
  - 但项目已经具备“文件夹展开 -> 云文档缓存 -> RAG 召回”的完整代码链路，本地若授权充分即可直接验证

## 11:05

### 修改内容描述

- 把云端知识入口从偏 `docs` 的命名，抽成更通用的 `sources/knowledge` 语义：
  - 新增 `knowledge/lark-cloud-knowledge.json`
  - 新增 `knowledge/lark-cloud-knowledge.example.json`
- 入口参数新增通用写法：
  - `--retriever-sources-file`
  - `LARK_RETRIEVER_SOURCES_FILE`
- 旧写法继续兼容：
  - `--retriever-docs-file`
  - `LARK_RETRIEVER_DOCS_FILE`
- 更新 `src/main.js`、`src/cliWatchMain.js`、`src/chatEventMain.js`、`src/chatPollMain.js`、`src/demoCliWatchLlmMain.js`
- 更新 `src/core/knowledge/retriever.js`
  - 优先解析通用 sources 文件
  - 若不存在再回退解析旧的 docs 文件

### 设计结论

- 现在的接口不是“calendar 专用”或“某个 folder 专用”
- 通用约定是：一个 manifest 文件里声明 `docs[]` 和 `folders[]`
- 后续无论接入多少个飞书文件夹，只要继续往 manifest 里加条目即可复用同一套加载、缓存和 RAG 链路

## 11:35

### 修改内容描述

- 重构 `knowledge/` 目录，只保留三类顶层内容：
  - `knowledge/skills/`
  - `knowledge/lark-cli-errors.json`
  - `knowledge/lark-cloud-knowledge.json`
- 删除冗余的旧云知识文件：
  - `knowledge/lark-cloud-docs*.json`
  - `knowledge/lark-cloud-knowledge.example.json`
- 更新根目录 `.env`
  - 新增 `LARK_RETRIEVER_SOURCES_FILE=knowledge/lark-cloud-knowledge.json`
  - 新增云知识缓存相关默认值
- 统一 README 和产品定义口径：
  - 只对外推荐 `lark-cloud-knowledge.json`
  - 只对外推荐 `--retriever-sources-file` / `LARK_RETRIEVER_SOURCES_FILE`

### 设计结论

- `knowledge/` 目录本身不再承载“示例文件堆叠”，只承载真正运行时需要的知识源
- manifest 仍然是通用接口，既能接单篇飞书文档，也能接任意数量的飞书文件夹

## 11:50

### 修改内容描述

- 将用户提供的云端 calendar skill 文件夹正式写入通用云知识清单：
  - `knowledge/lark-cloud-knowledge.json`
  - folder: `https://jcneyh7qlo8i.feishu.cn/drive/folder/I7w5fa75ylE0QUdswB9c0oLinnf`

### 测试计划

- 先验证 `drive files list` 能否列出该 folder
- 再验证 `npm run demo -- --source examples/lark-cloud-calendar-samples.jsonl --retriever-sources-file knowledge/lark-cloud-knowledge.json --no-quality-report` 能否把该 folder 展开的文档纳入 retriever

## 12:10

### 修改内容描述

- 扩展云知识入口，支持“云端 skill 文件夹”：
  - folder 中的 `SKILL.md`
  - `references/*.md`
  - 以及其他递归子目录里的 `.md` 文件
- 更新 `src/adapters/knowledge-source/larkDocsKnowledgeSource.js`
  - 递归列出 folder
  - 对 `type=file` 且扩展名为 `.md` 的文件使用 `drive +download`
  - 对 `doc/docx/wiki` 继续走 `docs +fetch`
  - 增加重试与单文件失败跳过
- 更新 `src/core/knowledge/retriever.js`
  - 将下载到的云端 markdown skill 文件按 chunk 进入 RAG
  - 新增 `LARK_RETRIEVER_DISABLE_INSTALLED_SKILLS=1` 时禁用 `~/.agents/skills` 回退
- 更新 `.env`
  - 增加 `LARK_RETRIEVER_DISABLE_INSTALLED_SKILLS=0`

### 测试与验证

- 已验证：
  - `node --check src/adapters/knowledge-source/larkDocsKnowledgeSource.js`
  - `node --check src/core/knowledge/retriever.js`
  - `npm run demo -- --source examples/lark-cloud-calendar-samples.jsonl --retriever-sources-file knowledge/lark-cloud-knowledge.json --no-quality-report`
- 本次关键结果：
  - 已真实命中云端来源：
    - `https://jcneyh7qlo8i.feishu.cn/file/BdDebqPKqonzqtxvo4Pc1AJHn6g`
    - `https://jcneyh7qlo8i.feishu.cn/file/KNcgbOxZEoZTNcxJ3NBcqRegnPe`
  - 说明云端 calendar skill 文件夹已被成功纳入 RAG

## 12:40

### 修改内容描述

- 对比赛版项目做结构化整理，不改动核心 matcher / retriever / composer 行为：
  - 新增 `docs/architecture.md`，说明当前主链路、入口分层、知识源边界和配置层次
  - 新增 `docs/demo-script.md`，固化比赛演示脚本：CLI 终端触发、飞书群事件触发、云知识验证
  - 重写 `docs/repo-structure.md`，同步当前真实目录结构
  - 更新 `README.md`，补充比赛主入口、当前工作流、知识源说明和旧参数兼容说明
  - 更新 `docs/product-definition.md`，新增当前比赛版交付边界
  - 更新 `package.json`，新增比赛推荐脚本：
    - `demo:competition:cli`
    - `demo:competition:chat`
    - `demo:cloud-calendar`
  - 更新 `config/knowledge-sources.example.json`，改为通用云知识 manifest 示例
  - 在 `src/core/knowledge/retriever.js` 中补充兼容说明注释，明确推荐使用 `knowledge/lark-cloud-knowledge.json`

### 修改原因

- 当前项目已经具备最小可运行版本能力，但入口、配置和文档说明较分散。
- 本轮目标是让比赛交付结构更清楚，让新接手的人能快速理解：
  - 知识源从哪里来
  - 事件从哪里进
  - 核心处理链路在哪里
  - 卡片从哪里出
  - 哪些命令用于比赛演示

### 影响范围

- 不改变现有核心行为。
- 新增的 npm scripts 只是对现有入口的命名收敛。
- 旧入口和旧参数继续保留，用于兼容历史 demo 与调试链路。

### 测试与验证

- 已通过：
  - `node --check src/core/knowledge/retriever.js`
  - `node --check src/demoCliWatchLlmMain.js`
  - `node --check src/chatEventMain.js`
  - `node -e "JSON.parse(...)"` 验证 `package.json`、`config/knowledge-sources.example.json`、`knowledge/lark-cloud-knowledge.json`
  - `npm run demo -- --source examples/lark-cli-rag-samples.jsonl --no-quality-report --no-live-help`
  - `npm run demo:cloud-calendar`
- `demo:cloud-calendar` 输出中已看到飞书云端文件来源：
  - `https://jcneyh7qlo8i.feishu.cn/file/BdDebqPKqonzqtxvo4Pc1AJHn6g`
  - `https://jcneyh7qlo8i.feishu.cn/file/KNcgbOxZEoZTNcxJ3NBcqRegnPe`

## 18:20

### 修改内容描述

- 新增官方 Node SDK WebSocket 长连接事件源：
  - `src/adapters/event-source/larkWsEventSource.js`
  - 通过 `@larksuiteoapi/node-sdk` 的 `WSClient` 和 `EventDispatcher` 订阅 `im.message.receive_v1`
  - SDK handler 只做快速事件归一化，并通过后台异步任务继续执行 RAG / LLM / 推卡，避免阻塞飞书 3 秒事件处理窗口
- 改造 `src/chatEventMain.js`
  - 新增 `--event-source sdk|lark-cli`
  - 新增 `--app-id`、`--app-secret`、`--sdk-log-level`
  - 默认可通过 `.env` 的 `LARK_EVENT_SOURCE`、`LARK_APP_ID`、`LARK_APP_SECRET` 配置官方 SDK 长连接
  - 保留原 `lark-cli event +subscribe` 作为 fallback
- 更新 `.env`
  - 新增官方长连接配置
  - `LARK_EVENT_SOURCE=sdk`
- 更新文档：
  - `README.md`
  - `docs/architecture.md`
  - `docs/demo-script.md`
  - `docs/product-definition.md`
- 新增依赖：
  - `@larksuiteoapi/node-sdk`

### 修改原因

- 官方建议比赛作品优先使用飞书长连接模式接收事件，避免公网 IP / 域名 / 内网穿透要求。
- SDK 长连接更适合在报告和演示中说明“本地或 Docker 容器即可接收飞书事件”。
- 原有 `lark-cli event +subscribe` 已经可用，因此本轮只新增事件源适配，不替换知识处理主链路。

### 影响范围

- 群事件主入口仍为 `src/chatEventMain.js`。
- 知识处理、RAG、LLM Composer、终端卡片和飞书卡片分发逻辑不变。
- 同一应用仍需避免同时启动多个 SDK / CLI 订阅客户端，否则会因飞书长连接集群模式导致事件分流。

### 测试与验证

- 已通过：
  - `node --check src/adapters/event-source/larkWsEventSource.js`
  - `node --check src/chatEventMain.js`
  - `JSON.parse` 验证 `package.json` 与 `package-lock.json`
- 已短启动验证：
  - `node src/chatEventMain.js --event-source sdk --source-chat-id oc_d32c2e3e2eb66b2efca3ef677620233e --no-reconcile-poll --push-level none`
  - 日志显示 `sdk websocket connected` 与 `ws client ready`

## 18:45

### 修改内容描述

- 重写 `README.md`，让它贴合当前项目版本：
  - 明确当前定位为方向 C 的 CLI 智能知识助手
  - 说明当前主链路：终端/群消息 -> 规则匹配 -> RAG -> 动态 help -> LLM Composer -> 终端/飞书卡片
  - 梳理推荐测试顺序：
    - `npm run demo:competition:cli`
    - `npm run demo:cli-shell`
    - `npm run demo:competition:chat`
    - `npm run demo:cloud-calendar`
  - 增加 npm scripts 说明
  - 增加参数说明，覆盖通用知识参数、CLI Watch、群事件、群轮询、LLM、推送参数
  - 增加云知识 manifest 示例

### 修改原因

- 旧 README 混合了多个阶段的历史说明，和当前 SDK 长连接、云知识、LLM Composer、CLI Watch 主线不够贴合。
- 当前需要进入主链路测试和 Prove it 阶段，README 应优先服务“怎么跑、怎么配、怎么测”。

### 影响范围

- 只修改文档，不改变运行逻辑。

### 测试与验证

- 已检查：
  - `rg` 验证 README 中推荐脚本和关键参数能在 `package.json` / `src` 中找到
  - `node -e` 验证 `package.json` JSON 合法
  - `node -e` 读取 README，确认中文内容正常

## 20:45

### 修改内容描述

- 新增 30 条 Agent 质量评测集：
  - `examples/evaluation/agent-quality-cases.jsonl`
- 新增 npm script：
  - `npm run eval:quality`
- 新增评测说明：
  - `docs/evaluation.md`
- 更新 `README.md`：
  - 增加评测集 / Prove it 说明
  - 在 npm scripts 中补充 `eval` 与 `eval:quality`
- 调整规则边界：
  - `command-existence-check-first` 不再匹配 `unknown flag`
  - 避免泛化命令存在性规则抢占 docs / shortcut 参数规则
- 同步修正小回归集中过时的 docs unknown flag 预期。

### 修改原因

- 当前需要构建可证明 Agent 能力的高质量评测集，而不是只做 E2E 冒烟测试。
- 评测集需要覆盖规则直达、本地 Skill RAG、云端知识 RAG，并能实际验证召回是否达到预期。

### 影响范围

- 不改变主链路入口。
- 新增评测集和脚本只用于离线评测，不真实推送飞书消息。
- 规则边界调整会让 `unknown flag` 更稳定地路由到 docs / shortcut 参数诊断，而不是泛化命令存在性诊断。

### 测试与验证

- 已通过：
  - `node -e` 验证 `examples/evaluation/agent-quality-cases.jsonl`
  - `node -e` 验证 `package.json`
  - `npm run eval`
    - 12/12 通过
  - `npm run eval:quality`
    - 30/30 通过
    - Match kind accuracy: 100.0%
    - Route skill hit rate: 100.0%
    - Source hit rate: 100.0%
  - 已复查部分易混淆样本的 Top3 召回，并调整问题表达，使 shared、im、contact、approval 等目标 Skill 更稳定出现在 Top1/Top3。

## 21:20

### 修改内容描述

- 新增 10 条 LLM Composer 复杂评测集：
  - `examples/evaluation/llm-composer-cases.jsonl`
- 新增 npm script：
  - `npm run eval:llm`
- 扩展 `src/evaluateMain.js`：
  - 支持 `expected_compose_mode`
  - 输出 `composeModeAccuracy`
  - 在 Markdown case detail 中显示 `compose=llm/template/...`
- 更新文档：
  - `docs/evaluation.md`
  - `README.md`

### 修改原因

- 原有 `eval:quality` 默认使用 template composer，只验证规则 / RAG / 决策链路，没有真实调用 LLM。
- 需要补充一组复杂样本，证明模型真实参与知识卡压缩，并且没有落到 template fallback。

### 影响范围

- 不改变主链路行为。
- 新增评测脚本会真实调用 `.env` 中配置的 LLM provider。
- 如果模型超时、返回非 JSON 或缺少 API Key，`eval:llm` 会失败，便于暴露真实接入问题。

### 测试与验证

- 已通过：
  - `node --check src/evaluateMain.js`
  - `node -e` 验证 `examples/evaluation/llm-composer-cases.jsonl`
  - `npm run eval`
    - 12/12 通过
  - `npm run eval:quality`
    - 30/30 通过
  - `npm run eval:llm`
    - 10/10 通过
    - Match kind accuracy: 100.0%
    - Route skill hit rate: 100.0%
    - Source hit rate: 100.0%
    - Compose mode accuracy: 100.0%

## 21:55

### 修改内容描述

- 新增统一评测入口：
  - `src/evaluationSuiteMain.js`
  - `npm run eval:all`
- 扩展 `src/evaluateMain.js`：
  - 新增 `--show-top3` 调试选项
  - 控制台输出每条 RAG 样本的 Top1/Top2/Top3 标题、Skill、分数和来源
  - Markdown 报告在 `--show-top3` 时追加 Top3 召回明细
- 更新文档：
  - `docs/evaluation.md`
  - `README.md`

### 修改原因

- 用户希望把小回归、质量评测和 LLM 评测整合为一条测试路径。
- 评测调试时需要直接观察 Top3 召回结果，判断召回是否只是“勉强命中”还是稳定靠前。

### 影响范围

- 不改变知识处理主链路。
- 新增统一 runner 仅编排现有评测脚本。
- `--show-top3` 只影响评测输出和报告，不影响匹配、召回和 LLM 行为。

### 测试与验证

- 已通过：
  - `node --check src/evaluateMain.js`
  - `node --check src/evaluationSuiteMain.js`
  - `node -e` 验证 `package.json`
  - `npm run eval:all -- --skip-llm --show-top3`
    - 2/2 suite 通过
    - 控制台可见 Top3 召回结果
  - `npm run eval:all`
    - 3/3 suite 通过
    - regression: 12/12
    - quality: 30/30
    - llm: 10/10

## 22:20

### 修改内容描述

- 优化评测终端输出：
  - `src/evaluateMain.js` 新增 `--summary-table` 与 `--quiet`
  - 每套评测可输出样本级汇总表
  - 表头包含 Result、Case、Group、Match、Expected、Rule / Top1 Evidence、Hit Skill、Source、Compose、Time
  - RAG 样本显示 Top1 证据标题，规则样本显示规则 ID
- 优化统一评测入口：
  - `src/evaluationSuiteMain.js` 默认传入 `--quiet --summary-table`
  - 新增 `--no-summary-table`
- 更新文档：
  - `docs/evaluation.md`
  - `README.md`

### 修改原因

- 原先 `--show-top3` 输出太像调试日志，不适合演示和人工快速判断。
- 需要在终端提供一张可读表格，让评委或用户能快速看到每条样本的预期、实际命中、来源、LLM 是否参与和耗时。

### 影响范围

- 只影响评测输出格式。
- 不改变规则匹配、RAG、LLM Composer 或主链路行为。

### 测试与验证

- 已通过：
  - `node --check src/evaluateMain.js`
  - `node --check src/evaluationSuiteMain.js`
  - `npm run eval:all -- --skip-llm`
    - 2/2 suite 通过
    - 终端表格显示正常
  - `npm run eval:all`
    - 3/3 suite 通过
    - regression: 12/12
    - quality: 30/30
    - llm: 10/10
    - LLM 表格中 Compose 全部为 `llm`

## 22:58

### 修改内容描述

- 使用 `lark-cli docs +fetch` 读取飞书阶段成果文档：
  - `https://jcneyh7qlo8i.feishu.cn/docx/XuPydaV9ko8vEGxwY3oc8HFvnLb`
- 按第三周期样式补充第四周期（5.2-5.4）内容：
  - 核心产出
  - 量化指标
  - 过程复盘与沉淀
  - 本周随手记
- 使用 `lark-cli docs +update --mode replace_range` 精准替换第四周期空模板，并保留第五周期标题和后续模板。

### 修改原因

- 需要同步当前比赛项目第四周期开发进度，沉淀 CLI 监听、SDK WebSocket、云知识 RAG、LLM Composer 和评测体系等阶段成果。

### 影响范围

- 仅更新飞书云文档内容。
- 不改动项目运行代码。

### 测试与验证

- 已通过 `lark-cli docs +fetch` 回读文档，确认第四周期内容已写入，且第五周期标题仍保留。
