# 2026-05-06

## 19:45 切回多模态 Embedding 默认模型

- 修改内容：
  - 将 `hybrid-vector` 默认 embedding 模型切回 `doubao-embedding-vision-250615`。
  - 默认走 `/embeddings/multimodal`，多模态模式继续使用 `{ type: "text", text }` 输入格式。
  - 多模态模式保持 batch size 为 1，避免该接口把多个文本 chunk 合并为一个多模态样本。
  - 保留文本模型兼容分支：显式配置非 vision/multimodal 文本模型时仍走 `/embeddings`、`encoding_format=float` 和 query instruction。
  - README 同步更新 `hybrid-vector` 配置说明。
- 修改原因：
  - 用户确认改用 `doubao-embedding-vision-250615` 多模态 embedding 模型，优先保证当前可用模型接入和本地向量缓存可运行。
- 影响范围：
  - 仅影响未显式配置 `ARK_EMBEDDING_MODEL` 时的 `hybrid-vector` 默认模型。
  - 当前 `.env` 已显式配置 `ARK_EMBEDDING_MODEL="doubao-embedding-vision-250615"`。
- 验证：
  - 已通过 `node --check src\\core\\knowledge\\retriever.js`。
  - 已确认 `.env` 当前为 `ARK_EMBEDDING_MODEL="doubao-embedding-vision-250615"`，且存在 `ARK_EMBEDDING_API_KEY`。

## 19:10 切换默认 Embedding 为纯文本模型

- 修改内容：
  - 将 `hybrid-vector` 默认 embedding 模型改为 `Doubao-embedding-large`。
  - 纯文本模型默认请求 `https://ark.cn-beijing.volces.com/api/v3/embeddings`，请求体使用 OpenAI-compatible 的 `input: string[]` 形式。
  - 保留 vision/multimodal 兼容：当模型名或 URL 包含 `vision` / `multimodal` 时，继续走 `/embeddings/multimodal` 和 `{ type: "text", text }` 输入格式。
  - 纯文本 embedding 默认 batch size 改为 16，多模态仍为 1。
  - 更新 README 中 `hybrid-vector` 的 embedding 配置说明。
- 修改原因：
  - 多模态 embedding 接口会把 `input` 作为一个整体样本返回单个向量，不适合批量 markdown chunk 嵌入。
  - 纯文本 embedding 更符合当前知识库内容形态，也更适合持久化本地向量缓存。
- 影响范围：
  - 仅影响 `--retriever-mode hybrid-vector`。
  - 默认 `keyword` 和 `hybrid` 不变。
- 验证：
  - 已通过 `node --check src\\core\\knowledge\\retriever.js`。
  - 已直接调用 Ark `/embeddings` 做 smoke test，当前 `.env` 中的 `Doubao-embedding-large` 返回 404：模型或 endpoint 不存在/无权限；代码会记录该错误并降级。
  - 已运行 `npm run eval:quality -- --retriever-mode hybrid-vector --quiet`，在 embedding 失败降级路径下质量集 32/32 通过。

## 19:30 对齐 Ark 官方文本 Embedding SDK 用法

- 修改内容：
  - 将默认文本 embedding 模型对齐为 `doubao-embedding-large-text-250515`。
  - embedding API Key 优先读取 `ARK_EMBEDDING_API_KEY`，未配置时回退到官方 SDK 示例使用的 `ARK_API_KEY`。
  - 文本 embedding 请求体新增 `encoding_format: "float"`。
  - query embedding 默认增加官方检索 instruction：`Instruct: Given a web search query, retrieve relevant passages that answer the query\nQuery: `。
  - README 同步更新 `hybrid-vector` 配置说明。
- 修改原因：
  - 官方 Python 示例使用 `client.embeddings.create(model="doubao-embedding-large-text-250515", input=..., encoding_format="float")`，并对 query 加 instruction；项目侧需要与该行为一致。
- 影响范围：
  - 仅影响 `hybrid-vector` 文本 embedding 请求。
  - 多模态/vision 兼容逻辑保留。
- 验证：
  - 已通过 `node --check src\\core\\knowledge\\retriever.js`。

## 18:05 搭建混合检索模式骨架

- 修改内容：
  - 在 `src/core/knowledge/retriever.js` 中保留原 `keyword` 排序，并新增 `hybrid` 排序模式。
  - `hybrid` 模式会分别计算 keyword raw、semantic-lite raw、route bonus，并做归一化后合成最终分数。
  - 新增动态权重：显式命令更偏 keyword，半结构化 API/身份问题居中，自然语言问题按 0.5/0.5 处理。
  - `npm run eval:all -- --show-top3 --retriever-mode hybrid` 可直接观察 Top3 中的 k/s/r 分项。
  - 主入口、群事件、轮询、CLI Watch、LLM demo 和评测入口均支持 `--retriever-mode <keyword|hybrid>` / `LARK_RETRIEVER_MODE`。
- 修改原因：
  - 用户希望先搭建混合检索架子，再通过现有综合评测集逐步优化召回，不立即引入复杂向量库。
- 影响范围：
  - 默认仍为 `keyword`，不改变已跑通演示链路。
  - 仅在显式开启 `hybrid` 时使用新归一化混合排序。
- 验证：
  - 已通过 `node --check` 验证 retriever、主入口、群监听、轮询、CLI Watch、demo 和评测入口语法。
  - 已运行 `npm run eval:quality -- --show-top3 --retriever-mode hybrid --quiet`，质量集 RAG/云知识样本 22/22 通过；剩余 10 个失败均来自当前 `knowledge rules: 0` 导致的规则直达样本失配。
  - 已运行 `npm run eval:all -- --show-top3 --retriever-mode hybrid --skip-llm`，可生成 Top3 k/s/r 分项；套件失败原因同上，规则文件为空。

## 18:45 接入 Ark Embedding 本地向量缓存

- 修改内容：
  - 在 `src/core/knowledge/retriever.js` 中新增 `hybrid-vector` 检索模式。
  - 接入 Ark multimodal embeddings API，默认模型为 `doubao-embedding-vision-251215`。
  - 新增本地持久化向量缓存，默认写入 `tmp/retriever-vector-store.json`；chunk 内容和模型不变时，后续启动直接复用。
  - 查询阶段只对用户 query 做一次 embedding，再和 keyword / vector semantic / route bonus 做归一化混合排序。
  - 当未配置 embedding key 或 embedding 失败时，自动降级为原 `semantic-lite`，并在评测、群监听、轮询和 CLI Watch 日志中输出 vector 状态。
- 修改原因：
  - 用户希望从“混合检索架子”继续推进到真实 embedding，但不想每次启动都重新嵌入全部知识。
  - 当前项目 chunk 数量只有几千级，本地持久化向量缓存比引入 Milvus 更轻，更适合比赛演示和本地 Docker 运行。
- 影响范围：
  - 默认 `keyword` 不变。
  - 只有显式设置 `--retriever-mode hybrid-vector` / `LARK_RETRIEVER_MODE=hybrid-vector` 时才启用向量链路。
  - 需要配置 `ARK_EMBEDDING_API_KEY`，可选配置 `ARK_EMBEDDING_MODEL`、`ARK_EMBEDDING_BASE_URL`、`LARK_RETRIEVER_VECTOR_STORE_FILE`。
- 验证：
  - 已通过 `node --check` 验证 retriever、事件、轮询、CLI Watch 和评测入口。
  - 已用 Ark embedding API 跑通 smoke test，确认本地向量缓存可写入并复用。
  - 已运行 `npm run eval:quality -- --retriever-mode hybrid-vector --quiet`，在未配置 embedding key 的降级路径下质量集 32/32 通过。

## 17:25 调整群事件触发规则：@ CLI 智能体必回复

- 修改内容：
  - 在 `src/chatEventMain.js` 和 `src/chatPollMain.js` 中新增结构化 mention 识别。
  - 当消息 `mentions` 或文本中 @ 命中 `飞书 CLI`、`飞书CLI`、`CLI 智能体`、`cli智能体` 时，绕过原有 `lark-cli` 文本规则，强制进入知识卡候选链路。
  - 新增 `LARK_CLI_AGENT_NAMES` 环境变量，可用逗号配置多个 CLI 智能体名称。
  - 更新 `README.md` 的 `.env` 示例和环境变量说明。
- 修改原因：
  - 用户反馈群里 @ 飞书 CLI 后，部分不含 `lark-cli` 字样的自然语言问题没有回复；这种场景应按“用户明确点名智能体”处理，必须触发知识卡。
- 影响范围：
  - 仅影响群事件监听和轮询补偿的候选消息过滤逻辑。
  - 不改变 SDK 连接、reconcile、matcher、retriever、LLM composer 或飞书推送策略。
- 验证：
  - 已通过 `node --check src\\chatEventMain.js`。
  - 已通过 `node --check src\\chatPollMain.js`。

## 16:45 恢复飞书知识卡 `release/debug` 展示模式

- 修改内容：
  - 在 `src/adapters/output/larkCardPayload.js` 中恢复 `release/debug` 双视图。
  - `release` 默认只展示快速判断、处理建议、建议命令和 Skill/模式信息。
  - `debug` 保留原完整卡片内容，包括概览、执行摘要、召回证据和上下文节选。
  - 在 `src/main.js`、`src/chatEventMain.js`、`src/chatPollMain.js`、`src/cliWatchMain.js`、`src/evaluateMain.js`、`src/demoCliWatchLlmMain.js` 中新增 `--card-view <release|debug>`，并支持 `LARK_CARD_VIEW`。
  - 在 `src/app/processKnowledgeEvent.js`、`src/app/runDemo.js` 中透传卡片展示配置。
  - 更新 `README.md` 中 `.env` 示例和参数说明。
- 修改原因：
  - 用户回退代码后，需要重新恢复精简版知识卡，避免飞书群内卡片过长、信息过散，影响演示和日常排查效率。
- 影响范围：
  - 仅影响飞书卡片展示层和入口参数解析，不改变 SDK 监听、reconcile 补偿、matcher、retriever、LLM composer 或推送策略。
  - 默认视图为 `release`，需要完整排障信息时显式使用 `--card-view debug`。
- 验证：
  - 已通过 `node --check` 验证 `larkCardPayload.js`、`processKnowledgeEvent.js`、`runDemo.js`、`main.js`、`chatEventMain.js`、`chatPollMain.js`、`cliWatchMain.js`、`evaluateMain.js`、`demoCliWatchLlmMain.js`。
  - 已用临时脚本验证：`release` 输出 5 个卡片元素，`debug` 输出 9 个卡片元素，副标题会显示对应视图。

## 03:15 修正 IM 发消息场景的召回排序与建议命令

- 修改内容：
  - 在 `src/core/knowledge/retriever.js` 中新增发送消息意图识别，对“通过 lark-cli 在飞书群里发消息”等问题提升 `lark-im` 的 `+messages-send` 证据权重。
  - 当召回结果包含 `lark-im` 且问题表达为发消息意图时，建议命令改为 `lark-cli im +messages-send --chat-id <oc_xxx> --text "<消息内容>" --as bot`。
- 修改原因：
  - 原召回容易被通用 workflow/base 证据干扰，导致知识卡只建议 `lark-cli im --help`，没有直接给出可执行的发送消息命令。
  - 本地 `lark-im` skill 的真实发送消息入口是 `+messages-send`，不是 `+send`。
- 影响范围：
  - 仅影响本地轻量 RAG 排序和 `next_command_template` 推断，不改变 matcher、LLM composer 或飞书推送链路。
- 验证：
  - 已通过 `node --check src\core\knowledge\retriever.js`。
  - 已运行质量评测片段，`rag-03-im-send-message` Top1 命中 `knowledge\skills\lark-im\references\lark-im-messages-send.md` 的 `im +messages-send Commands`。

## 02:06 强制 LLM 知识整合通道与 IM/Event 召回修正

- 修改内容：
  - 在 `src/main.js`、`src/chatEventMain.js`、`src/chatPollMain.js`、`src/cliWatchMain.js`、`src/evaluateMain.js` 中新增 `--force-llm-compose` 参数。
  - 新增 `LARK_FORCE_LLM_COMPOSE=1` 环境变量入口，用于将默认知识卡整理模式切换为 LLM。
  - 保留原有 `--compose-mode <template|llm|off>` 与 `--no-compose` 行为，命令行参数仍可覆盖环境变量。
  - 调整 `src/core/knowledge/retriever.js` 的 skill 路由信号，增强“群消息读取 / 群消息监听 / 长连接事件”对 `lark-im` 和 `lark-event` 的识别，并对非首选 skill 做轻量降权。
  - 在 `examples/evaluation/agent-quality-cases.jsonl` 中新增 IM 群消息读取和 Event 长连接监听两条评测样本。
  - 更新 `README.md` 中的 `.env` 示例、CLI Shell 示例、群事件监听示例、LLM/Composer 参数说明和质量评测数量。
  - 更新 `docs/evaluation.md` 的质量评测数量说明。
- 修改原因：
  - 复杂问题仅靠规则或 RAG 召回容易出现解释方向偏差，需要提供一个明确通道，强制让 LLM 参与 rules / RAG / help 证据整合。
  - 用户反馈“读取群里的信息”场景误召回到 `lark-contact`，需要将该类问题优先路由到 `lark-im`；实时监听场景优先路由到 `lark-event`。
- 影响范围：
  - 影响所有主入口的参数解析默认值，但不改变 matcher、retriever、composer 的核心执行链路。
  - 未设置 `LARK_FORCE_LLM_COMPOSE=1` 且未传 `--force-llm-compose` 时，默认仍保持 template composer。
  - 新增评测样本会使质量集总数变为 32 条。
- 验证：
  - 已通过 `node --check` 验证相关入口与 retriever 语法。
  - 已验证 `examples/evaluation/agent-quality-cases.jsonl` 32 条 JSONL 可解析。
  - 已运行质量评测，新增 `rag-18-im-read-chat-messages` 命中 `lark-im`，`rag-19-event-listen-chat-messages` 命中 `lark-event`。
  - 当前 `knowledge/lark-cli-errors.json` 工作区内容为 `{}`，导致规则直命中样本显示 `knowledge rules: 0` 并失败；该文件属于已有工作区改动，本次未擅自恢复。

## 02:20 修复 SDK 群事件监听重复出卡

- 修改内容：
  - 调整 `src/chatEventMain.js` 的参数解析：当 `--event-source sdk` 时，如果用户没有显式传入 `--reconcile-poll` / `--no-reconcile-poll`，默认关闭历史消息轮询补偿。
  - 保留 `lark-cli` 事件源下默认开启 reconcile 的旧行为。
  - 更新 `README.md` 中群事件参数说明，明确 `sdk` 模式默认不启用 reconcile，`lark-cli` 模式默认启用。
- 修改原因：
  - 官方 SDK WebSocket 已经成为主监听链路，继续默认启用轮询补偿会导致同一条群消息可能被 SDK 和轮询各处理一次。
  - 用户调试时常加 `--push-bypass-dedupe`，会绕过推送层去重，使重复处理直接表现为两张知识卡片。
- 影响范围：
  - 只影响 `src/chatEventMain.js` 的 SDK 事件监听默认行为。
  - 如果确实需要在 SDK 模式下继续做轮询补偿，可以显式加 `--reconcile-poll`。
- 验证：
  - 已进行语法检查。

## 02:40 增强 LLM Composer 可观测性与中文召回路由稳定性

- 修改内容：
  - 在 `src/app/processKnowledgeEvent.js` 中新增 composer 状态日志，输出 `requested`、`actual`、`model` 与 fallback 原因。
  - 在 `src/adapters/output/larkCardPayload.js` 中新增 `Composer` 区块，飞书卡片会直接展示 `mode/model/fallback`。
  - 将 `src/core/knowledge/retriever.js` 的中文 skill 路由提示改为 Unicode escape 常量，避免中文正则在不同终端/编码下变成乱码后失效。
- 修改原因：
  - 用户反馈 `--force-llm-compose` 后飞书卡片看起来仍像 template，缺少判断 LLM 是否真实参与的证据。
  - “读取群里的信息”仍召回 `contact +get-user`，需要确保中文 IM/Event 查询稳定命中 `lark-im` / `lark-event`。
- 影响范围：
  - 不改变 composer 调用策略，只增加运行日志和卡片展示字段。
  - 召回路由更稳定，但仍保留原有轻量 RAG 排序机制。
- 验证：
  - 已通过 `node --check` 验证 `retriever.js`、`larkCardPayload.js`、`processKnowledgeEvent.js`。
  - 已运行质量评测片段，`rag-18-im-read-chat-messages` Top1 为 `lark-im chats`，`rag-19-event-listen-chat-messages` Top1 为 `event +subscribe Commands`。

## 02:55 强制 LLM 模式禁止 template fallback 推送

- 修改内容：
  - 将 `--force-llm-compose` 的语义从“优先尝试 LLM”调整为“只允许 LLM 成功整理后的知识卡推送”。
  - 在 `src/main.js`、`src/chatEventMain.js`、`src/chatPollMain.js`、`src/cliWatchMain.js`、`src/evaluateMain.js` 中新增 `requireLlmCompose` 运行选项。
  - 当 `--force-llm-compose` 或 `LARK_FORCE_LLM_COMPOSE=1` 启用时，同时设置 `composeMode=llm` 与 `requireLlmCompose=true`。
  - 在 `src/app/processKnowledgeEvent.js` 中新增强制闸门：如果 LLM 调用失败并回退到 `template_fallback`，则只输出 `[composer][blocked]` 日志，不构建飞书卡片、不推送到群。
  - 更新 `README.md` 中 `LARK_FORCE_LLM_COMPOSE` 与 `--force-llm-compose` 的说明。
- 修改原因：
  - 用户要求强制 LLM 知识整合时，只准返回 LLM 调用成功后的知识卡片，不能把 template fallback 卡片推到群里。
- 影响范围：
  - 影响所有主入口中 `--force-llm-compose` 的语义。
  - `--compose-mode llm` 仍保持原语义：LLM 失败时允许 template fallback；`--force-llm-compose` 才是严格模式。
- 验证：
  - 已进行语法检查。

## 03:05 回退强制 LLM 严格阻断与 SDK reconcile 默认关闭

- 修改内容：
  - 应用户要求，回退到“两个知识卡片召回”问题排查之前的运行语义。
  - `--force-llm-compose` 恢复为仅设置 `composeMode=llm`，不再额外要求 `requireLlmCompose=true`。
  - 移除 `processKnowledgeEvent` 中因 LLM fallback 而阻断飞书卡片推送的逻辑。
  - 移除飞书卡片中的 `Composer` 展示区块与 composer 状态日志。
  - 恢复 `chatEventMain` 中 SDK 模式默认 reconcile 补偿行为。
  - 同步恢复 `README.md` 中 `--force-llm-compose` 与 `--reconcile-poll` 的说明。
- 修改原因：
  - 用户确认当前无输出问题来自旧监听进程排查后的行为改动，要求回退到本轮问题之前的版本。
- 影响范围：
  - 恢复此前“LLM 失败可 fallback 并继续推送卡片”的行为。
  - SDK 事件监听再次默认开启 reconcile 补偿，若存在旧监听进程或去重绕过，仍可能出现重复卡片。
- 验证：
  - 已进行语法检查。
## 16:40 调整终端 Debug 卡片，显式展示混合检索过程

- 修改内容：
  - 调整 `src/core/knowledge/retriever.js`，在 `_retrieval` 中补充 `retrieverMode` 和原始 `queryText`，让上层输出能区分真实用户问题与 LLM 整理查询。
  - 重写 `src/adapters/output/terminalCard.js` 的 Debug 视图文案，去除原有乱码文本，统一改为可读中文。
  - 在召回结果 Top3 中新增混合检索分项展示：`mode / k / s / r`，用于区分关键词分、语义分和路由补偿分。
  - 在 RAG 命中时新增以下字段：
    - `检索模式`
    - `用户问题`
    - `LLM 整理查询`
    - 每条证据的 `hybrid-vector k=... s=... r=...`
- 修改原因：
  - 用户反馈当前群事件 Debug 输出只能看到“召回结果”，但看不出是否真的走了 `hybrid` / `hybrid-vector`，也看不出 LLM 查询串和原始问题之间的关系。
  - 旧版 `terminalCard.js` 存在乱码，影响比赛演示和日常排障。
- 影响范围：
  - 仅影响终端 Debug 观察体验，不改变 matcher / retriever / composer 的主行为逻辑。
  - 飞书卡片 release 视图不受影响，主要增强本地终端排障信息。
- 验证：
  - 已通过 `node --check src/adapters/output/terminalCard.js`
  - 已通过 `node --check src/core/knowledge/retriever.js`

## 17:05 重构关键词匹配逻辑，按 skill 文档结构建立关键词索引

- 修改内容：
  - 调整 `src/core/knowledge/retriever.js` 的 `tokenize()`，去掉中文单字匹配，只保留 2~4 字关键词片段和英文/命令 token。
  - 在 `normalizeSearchText()` 中清理群消息里的 `@_user_x` / `<at>` 提及文本，避免 mention 噪声进入检索。
  - 新增 `buildChunkKeywordText()`，让 keyword 索引优先来自 skill 文档的 `skillName / 标题 / section / references 文件名 / 抽取出的 lark-cli 命令`，不再直接用正文全文做关键词散打。
  - 调整 `phraseBonus()` 和 `commandIntentBonus()` 的作用对象，让关键词排序更多依赖技能文档结构化字段。
  - 补强 `inferPreferredSkills()`、`hasSendMessageIntent()`、`hasReadChatMessageIntent()` 的 token 级判断，提升 `群发消息 / 读群消息` 对 `lark-im` 的路由稳定性。
- 修改原因：
  - 用户反馈群聊问句的 Top3 命中词出现 `发 / 消 / 息 / 群` 这类单字，导致观感混乱，也容易把无关 chunk 错抬上来。
  - 原 keyword 逻辑对 skill 文档正文过于敏感，不够像“根据技能文档标题和命令族来检索”。
- 影响范围：
  - 影响本地 keyword / hybrid / hybrid-vector 的关键词侧排序逻辑。
  - 向量检索和 LLM 整理链路保持不变，只是 keyword 侧更干净、更偏 skill 文档结构。
- 验证：
  - 已通过 `node --check src/core/knowledge/retriever.js`

## 17:20 修复 CLI Shell 成功态帮助输出未触发知识卡

- 修改内容：
  - 调整 `src/cliWatchMain.js` 的 `shouldAnalyzeCommand()`，新增 `hasRecoverableCliGuidance()` 分支。
  - 当 `lark-cli` 命令虽然退出码为 `0`，但 stdout 明显出现 `Available Commands:` / `Usage:` / `Use "lark-cli ..."` 这类恢复性提示时，也会进入知识分析链路。
  - 该分支专门识别“用户把子命令写成裸词而不是 `+subcommand`”的场景，例如：`lark-cli contact search-user`。
- 修改原因：
  - 用户在 `demo:cli-shell` 中输入 `lark-cli contact search-user` 后，CLI 返回帮助和候选子命令，但由于退出码为 `0`，原逻辑直接判定为 `skip`，导致既没有终端知识卡，也没有飞书群推送。
- 影响范围：
  - 影响 `demo:cli-shell` / `demo:cli-watch` / `demo:competition:cli` 的命令触发条件。
  - 不改变真正报错场景的处理逻辑，只补充“成功退出但明显需要纠错引导”的情况。
- 验证：
  - 已通过 `node --check src/cliWatchMain.js`
